apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: messenger
  labels:
    app: chat-service
spec:
  type: ClusterIP
  selector:
    app: chat-service
  ports:
    - name: http
      protocol: TCP
      port: 8083
      targetPort: 8083
