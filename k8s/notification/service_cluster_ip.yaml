apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: messenger
  labels:
    app: notification-service
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: http
      protocol: TCP
      port: 8085
      targetPort: 8085
